=======[❌ TEORIA ]=================================================================================
Fundamento técnico vCPU:

- El overcommit de CPU es posible porque Proxmox/KVM permite asignar más vCPUs que cores físicos.
- Linux KVM/QEMU y el scheduler del host manejan la distribución del tiempo de CPU entre las VMs, gestionando el acceso concurrente.

Fundamento técnico Cluster:

- En Proxmox VE (que utiliza Corosync como capa de clustering), el “quorum” es el mecanismo que asegura que sólo un subconjunto mayoritario de nodos pueda tomar decisiones válidas sobre el estado del clúster y sus recursos. 

=======[❌ CREAR MÁQUINA VIRTUAL ]==================================================================
# buena práctica para mejor visualización
⚙️ > Sort Key: Name

# creo una nueva máquina virtual
sobre el Node indicado > Create VM

# para crear una máquina virtual o contenedor es recomendable hacerlo desde:
local-lvm


# Ajustes al crear la máquina:
Create VM
------------------------------------------------------------------------------
+ General:
*********************************************************
Name: ~
_
+ OS:
*********************************************************
Storage: Synology (para producción) | local (para pruebas o desarrollo)
OS: (selecciono la última versión de cada S.O)
_
+ System:
*********************************************************
_
+ Disk:
*********************************************************
Bus/Devicec: SATA | 0
Storage: Synology (para producción) | local (para pruebas o desarrollo)
Disk Size: 40 GiB (o los necesarios)
Cache: Write Back
[x] Avanced
~
[x] SSD emulation
_
+ CPU:
*********************************************************
# es recomendable poner un socket y varios cores
Sockets: 1 
Cores: 4
_
+ Memory:
*********************************************************
Memory (MiB): 2048 (o los que sean necesarios, en este caso 2GB | 4096 = 4GB) 
_
+ Network:
*********************************************************
Bridge: vmbr0
VLAN Tag: (el tag de vlan correspondiente no la vlan de red)
Model: 
------------------------------------------------------------------------------





===============================================================================
                                ASIGNACIÓN DE CPU EN PROXMOX
===============================================================================
# Cores asignados en Proxmox: vCPUs que configuras para cada VM
-------------------------------------------------------------------------------

-------------------------------------------------------------------------------

Método             | ¿CPU física directa?         | Descripción técnica
-------------------|------------------------------|-------------------------------------------------------------
Asignar vCPU       | ❌ No                       | Lo más común. El hipervisor distribuye el uso entre vCPUs.
CPU pinning        | ✅ Parcialmente             | Asocia vCPUs a cores físicos específicos. Ejemplos: taskset, numactl, configuración en XML/QEMU args.
CPU passthrough    | ✅ Sí, pero para PCI        | Passthrough para hardware PCI (GPU, etc.). No aplica para CPU.


-------------------------------------------------------------------------------
Recomendación para un host con 4 cores físicos:
+---------------------------------+---------------------------------+
| Tipo de uso                     | Nº de VMs seguras (1 vCPU c/u)  |
+---------------------------------+---------------------------------+
| Carga baja (CLI, routers, logs) | 8 – 10                          |
| Carga mixta                     | 5 – 6                           |
| Carga alta (pentesting, GVM)    | 2 – 3 máximo simultáneas        |
+---------------------------------+---------------------------------+

-------------------------------------------------------------------------------
¿Cómo se ejecutan más vCPUs que cores físicos?

- Cada vCPU es una porción de tiempo (time slice) de un core físico.
- El scheduler del hipervisor intercala tareas en milisegundos.
- Si varias VMs no usan CPU intensivamente al mismo tiempo, funciona sin problemas.
- Si todas realizan carga pesada simultáneamente, se genera cola y latencia.

-------------------------------------------------------------------------------
Resumen clave:

✅ Puedes asignar tantas vCPUs como quieras.  
⚠️ Estás repartiendo tiempo de CPU, no creando potencia adicional real.  
🧠 Usa overcommit con prudencia: evalúa la carga, no solo el número de vCPUs.

-------------------------------------------------------------------------------
Consejos técnicos:

- Usa drivers **virtio** para disco y red, mejora rendimiento y compatibilidad.
- Habilita **ballooning** para RAM en laboratorios para mejor gestión dinámica.
- Para menos overhead, considera usar contenedores (LXC) en vez de VMs.
- Aplica **CPU limits y CPU shares** para controlar recursos si varias VMs se activan a la vez.

-------------------------------------------------------------------------------
Fundamento técnico:

- El overcommit de CPU es posible porque Proxmox/KVM permite asignar más vCPUs que cores físicos.
- Linux KVM/QEMU y el scheduler del host manejan la distribución del tiempo de CPU entre las VMs, gestionando el acceso concurrente.

===============================================================================
